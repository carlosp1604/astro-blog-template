---
import { t, getLocale } from '@/translations'

const lang = getLocale(Astro.url.pathname)

const siteName = import.meta.env.PUBLIC_SITE_NAME as string

const year = String(new Date().getFullYear())
const copyrightTranslation = await t(lang, 'common', 'footer_copyright_title', { siteName, year })
const homeLinkTranslation = await t(lang, 'common', 'footer_home_link_title')
---

<footer class="bg-app-surface text-app-text border-t border-app-border">
  <div class="container mx-auto px-4 py-8 flex flex-col md:flex-row justify-between items-center gap-4">
    <p class="text-sm">
      {copyrightTranslation}
    </p>

    <nav aria-label="Footer navigation" class="flex gap-4">
      <a href='/' class="text-sm hover:underline">
        {homeLinkTranslation}
      </a>
    </nav>
  </div>
</footer>
